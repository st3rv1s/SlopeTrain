<muclient>
  <plugin
    name="SlopeTrain"
    author="Kanaye"
    id="3668d613691c941bb5f2478c"
    language="Lua"
    purpose="A plugin to help you lead a slope train"
    save_state="y"
    date_written="2025-3-24 17:59:57"
    requires="5.07"
    version="1.27"
  >
  </plugin>

  <aliases>
    <!-- Alias for South direction (Chutes) -->
    <alias
      match="^ices$"
      name="south"
      enabled="y"
      regexp="y"
      send_to="12"
      sequence="100"
      script="enter_slope"
    />
    
    <!-- Alias for East direction (Hatchery) -->
    <alias
      match="^icee$"
      name="east"
      enabled="y"
      regexp="y"
      send_to="12"
      sequence="100"
      script="enter_slope"
    />
    
    <!-- Alias for West direction (Chutes) -->
    <alias
      match="^icew$"
      name="west"
      enabled="y"
      regexp="y"
      send_to="12"
      sequence="100"
      script="enter_slope"
    />
	
    <!-- Alias for checking update -->
    <alias
      match="^slopetrain update check$"
      enabled="y"
      regexp="y"
      sequence="99"
      script="update_check_alias"
    />
    
    <!-- Alias for installing update -->
    <alias
      match="^slopetrain update install$"
      enabled="y"
      regexp="y"
      sequence="99"
      script="update_install_alias"
    />
	
    <alias
      match="^icenxt$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="move_next_slope_room"
    />

    <!-- Alias to set slope notification messages -->
    <alias
      match="slopetrain set msg"
      enabled="y"
      sequence="99"
      script="set_slope_message"
    />

    <!-- Alias to set the notification channel -->
    <alias
      match="slopetrain channel *"
      enabled="y"
      sequence="99"
      script="set_channel"
    />

    <!-- Alias to set to repeat the direction -->
    <alias
      match="repdir"
      enabled="y"
      sequence="99"
      script="repeat_train_direction"
    />

    <!-- Alias to set to type of pass -->	
    <alias
      match="slopetrain set pass"
      enabled="y"
      sequence="99"
      script="toggle_pass_type"
    />

    <!-- Alias to show type of pass -->
    <alias
      match="slopetrain show pass"
      enabled="y"
      sequence="99"
      script="show_current_pass"
    />
	
    <!-- Alias to show helpfile -->
    <alias
      match="slopetrain help"
      enabled="y"
      sequence="99"
      script="show_helpfile"
    />

    <!-- Alias to turn off repop message -->
    <alias
      match="slopetrain repop"
      enabled="y"
      sequence="99"
      script="toggle_slope_repop"
    />
  </aliases>

  <script>
    <![CDATA[

require("wait")
require("async")
json = require("json")

plugin_url = "https://raw.githubusercontent.com/st3rv1s/SlopeTrain/refs/heads/main/SlopeTrain.xml"
SetVariable("DownloadURL", plugin_url)
plugin_protocol = "HTTPS"
plugin_prefix = "[SlopeTrain]"

function update_check_alias()
	update_plugin("check")
	ColourNote("white", "", plugin_prefix .. " Checking for updated version...")
end

function update_install_alias()
	update_plugin("install")
	ColourNote("white", "", plugin_prefix .. " Checking for and installing updated version...")
end

function reload_plugin()
	local scriptPrefix = GetAlphaOption("script_prefix")
	local retval

	-- If the user has not already specified the script prefix for this version of mush, pick a
	-- reasonable default value
	if scriptPrefix == "" then
		scriptPrefix = "\\\\\\"
		SetAlphaOption("script_prefix", scriptPrefix)
	end

	-- Tell mush to reload the plugin in one second.  We can't do it directly here because a
	-- plugin can't unload itself.  Even if it could, how could it tell mush to load it again
	-- if it weren't installed?
	retval = Execute(scriptPrefix .. "DoAfterSpecial(0.1, \"ReloadPlugin('" .. GetPluginID() .. "')\", sendto.script)")
end

function update_plugin(mode)
	update_mode = mode

	wait.make(get_plugin_file)
end

function get_plugin_file()
	local urlThread = async.request(plugin_url, plugin_protocol)

	if not urlThread then
		note_error("Couldn't create async url request.")
		return
	end

	local timeout = 10
	local totTime = 0
	while urlThread:alive() and totTime < timeout do
		wait.time(0.1)
		totTime = totTime + 0.1
	end

	local remoteRet, pluginData, status, headers, fullStatus = urlThread:join()

	if not status then
		ColourNote("red", "", plugin_prefix .. " Couldn't download plugin file. No status code.")

		return
	end

	if status ~= 200 then
		ColourNote("red", "", plugin_prefix .. " Plugin file request status code: " .. status .. ": " .. fullStatus)
		return
	end

	local currentVersion = GetPluginInfo(GetPluginID(), 19) or 0
	local currentVerStr = tostring(currentVersion)
	local remoteVerStr = string.match(pluginData, '%s%s+version="([0-9%.]+)"')
	local remoteVersion = tonumber(remoteVerStr or "") or 0

	if remoteVersion == currentVersion then
		ColourNote("white", "", plugin_prefix .. " You are running the most recent version (v" .. currentVerStr .. ")")
	elseif remoteVersion < currentVersion then
		ColourNote(
			"white",
			"",
			plugin_prefix .. " You have a newer version than is publicly available. (v" .. currentVerStr .. ")"
		)
	elseif update_mode == "check" then
		ColourNote(
			"white",
			"",
			plugin_prefix .. " You are running v" .. currentVerStr .. ", but there's a newer version v" .. remoteVerStr
		)
	elseif update_mode == "install" then
		ColourNote(
			"white",
			"",
			plugin_prefix .. " Updating plugin from version " .. currentVerStr .. " to version " .. remoteVerStr
		)

		local pluginFile = GetPluginInfo(GetPluginID(), 6)
		local file = io.open(pluginFile, "wb")
		file:write(pluginData)
		file:close()
		reload_plugin()
	else
		ColourNote("red", "", plugin_prefix .. " Invalid update mode: " .. update_mode)
	end
end
----------------------- End Plugin Update Code -----------------------

-- Ensure the gmcphelper module is loaded
require("gmcphelper")

-- Load the Aardwolf color functions	
dofile(GetInfo(60) .. "aardwolf_colors.lua")

-- Basic print function that supports Aardwolf, xterm, and ANSI color codes
function cnote(string)
    AnsiNote(stylesToANSI(ColoursToStyles(string)))
end

-- Show the helpfile after installing or loading the plugin
function OnPluginInstall()
	show_helpfile()
	
local saved_msgs = GetVariable("train_msg")
	if saved_msgs then
		local ok, decoded = pcall(json.decode, saved_msgs)
		if ok and type(decoded) == "table" and #decoded > 0 then
			train_msg = decoded
		else
			train_msg = {
				"@C(@DSlopeTrain@C) Default message moving @C%s@W!$C"
			}
		end
	else
		train_msg = {
			"@C(@DSlopeTrain@C) Default message moving @C%s@W!$C"
		}
	end
end



--
-- Character state constants (from kill_em_all_core.lua)
--
char_state_standing = 3
char_state_fighting = 8
char_state_sleeping = 9
char_state_resting = 11

--
-- Room state tracking
--
room = {
    id = 0,
    name = "",
    zone = ""
}

--
-- Character state tracking
--
character = {
    state = 0,
    previous_state = 0
}

--
-- Plugin state variables
--
is_group_leader = false
player_name = ""
on_slope = false
slope_repop_enabled = true
chan = GetVariable("chan") or "gtell"


--
-- Room ID constants
--
ROOM_PINNACLE = 38813

-- First rooms of each slope (entered from pinnacle)
ROOM_SLOPE_EAST_START = 36614
ROOM_SLOPE_SOUTH_START = 38825
ROOM_SLOPE_WEST_START = 38836

-- Last rooms of each slope
ROOM_SLOPE_EAST_END = 36623
ROOM_SLOPE_SOUTH_END = 36634
ROOM_SLOPE_WEST_END = 36845

slope_directions = {
	slingshot = "enter slingshot",
	kill = "west",
	hatchery = "east",
	chutes = "south",
}

slope_exits = {
	west = ROOM_SLOPE_WEST_START,
	east = ROOM_SLOPE_EAST_START,
	south = ROOM_SLOPE_SOUTH_START,
}

slope_rooms = {}

for i = 38814, 38845 do
	if i ~= 38824 and i ~= 38835 then
		slope_rooms[i] = true
	end
end

local status, result = pcall(json.decode, GetVariable("train_msg"))
if status and type(result) == "table" then
	train_msg = result
else
	Note("[SlopeTrain] No saved train messages located. Using defaults. Customize them with 'slopetrain set msg'.")
	train_msg = {
		"@C(@DSlopeTrain@C) @x256CHOO @x256CHOO@W, the train is moving @C%s@W!$C",
		"@C(@DSlopeTrain@C) @WALL ABOARD! The train is moving @C%s@W... @x256CHOO @x256CHOO@W!$C",
	}
	SetVariable("train_msg", json.encode(train_msg))
end

-- Helper to fill all %s placeholders with the same value
function repeat_sprintf(template, value)
    return template:gsub("%%s", value)
end

function OnPluginBroadcast(msg, id, name, text)
  if id == "3e7dedbe37e44942dd46d264" then
    if text == "char.base" then
      player_name = gmcp("char.base.name")
    elseif text == "group" then
      is_group_leader = (gmcp("group.leader") == gmcp("char.base.name"))
    elseif text == "room.info" then
      on_room_info_update(gmcp("room.info"))
    elseif text == "char.status" then
      on_character_status_update(gmcp("char.status"))
    end
  end
end

--
-- Room change detection
--
function on_room_info_update(room_info)
    local previous_room_id = room.id
    local previous_zone = room.zone

    room.id = tonumber(room_info.num)
    room.name = room_info.name
    room.zone = room_info.zone

    -- Update convenience flags
    is_in_icefall = (room.zone == "icefall")
    on_slope = slope_rooms[room.id] or false

    -- Detect room changes
    if previous_room_id ~= 0 and previous_room_id ~= room.id then
        on_room_change(previous_room_id, room.id)
    end

    -- Detect zone changes
    if previous_zone ~= "" and previous_zone ~= room.zone then
        on_zone_change(previous_zone, room.zone)
    end
end

-- Called when the player moves to a different room
function on_room_change(previous_room_id, new_room_id)
    if not is_group_leader then
        return
    end

    -- Announce when arriving at a slope start
    if new_room_id == ROOM_SLOPE_EAST_START then
        slope_success("east")
    elseif new_room_id == ROOM_SLOPE_SOUTH_START then
        slope_success("south")
    elseif new_room_id == ROOM_SLOPE_WEST_START then
        slope_success("west")
    end
end

-- Called when the player enters a different zone
function on_zone_change(previous_zone, new_zone)
    -- TODO: Add zone change logic here
    -- Example: disable features when leaving icefall
end

--
-- Character state change detection
--
function on_character_status_update(status)
    character.previous_state = character.state
    character.state = tonumber(status.state)

    -- Detect state transitions
    if character.previous_state ~= character.state then
        on_state_change(character.previous_state, character.state)
    end
end

-- Called when character state changes (fighting, standing, sleeping, etc.)
function on_state_change(previous_state, new_state)
    -- Entering combat
    if previous_state ~= char_state_fighting and new_state == char_state_fighting then
        on_enter_fighting_state()
    end

    -- Exiting combat
    if previous_state == char_state_fighting and new_state ~= char_state_fighting then
        on_exit_fighting_state()
    end
end

-- Called when the player enters combat
function on_enter_fighting_state()
    -- TODO: Add enter combat logic here
end

-- Called when the player exits combat (mob died, fled, etc.)
function on_exit_fighting_state()
    -- TODO: Add exit combat logic here
    -- Example: auto-loot, continue moving, resume train
end

-- Map direction names to slope start rooms
slope_start_rooms = {
    east = ROOM_SLOPE_EAST_START,
    south = ROOM_SLOPE_SOUTH_START,
    west = ROOM_SLOPE_WEST_START,
}

function enter_slope(name, line, wildcards)
    if character.state == char_state_fighting then
        Note("[SlopeTrain] You're in combat. Movement blocked.")
        return
    end

    local target_room = slope_start_rooms[name]
    if not target_room then
        Note("[SlopeTrain] Unknown direction: " .. name)
        return
    end

    -- Use mapper goto directly to the slope start
    -- Announcement happens in on_room_change when we arrive
    Execute("mapper goto " .. target_room)
end

function get_slope_direction(room)
  for dir, num in pairs(slope_exits) do
    if num == room then
      return dir
    end
  end
  return nil
end

function move_next_slope_room()
    -- Move to the next slope in order: east -> south -> west -> east (cycle)
    -- Only works when at the last room of a slope

    if room.id == ROOM_SLOPE_EAST_END then
        Note("[SlopeTrain] East slope complete. Moving to South slope...")
        Execute("mapper goto " .. ROOM_SLOPE_SOUTH_START)
    elseif room.id == ROOM_SLOPE_SOUTH_END then
        Note("[SlopeTrain] South slope complete. Moving to West slope...")
        Execute("mapper goto " .. ROOM_SLOPE_WEST_START)
    elseif room.id == ROOM_SLOPE_WEST_END then
        Note("[SlopeTrain] West slope complete. Moving to East slope...")
        Execute("mapper goto " .. ROOM_SLOPE_EAST_START)
    else
        Note("[SlopeTrain] You must be at the end of a slope to use this command.")
        Note("[SlopeTrain] Current room: " .. room.id)
        Note("[SlopeTrain] End rooms: East=" .. ROOM_SLOPE_EAST_END .. ", South=" .. ROOM_SLOPE_SOUTH_END .. ", West=" .. ROOM_SLOPE_WEST_END)
    end
end

function slope_success(direction)
    -- Use repeat_sprintf so all %s get the same direction
    local slope_msg = repeat_sprintf(train_msg[math.random(1, #train_msg)], direction:upper())
    last_train_msg = slope_msg
    SendNoEcho(chan .. " " .. slope_msg)
end

function follow_the_leader(name, line, wildcards)
	if wildcards[1] == gmcp("group.leader") then
		SendNoEcho(slope_directions[name])
	end
end

function expired_pass_action()
    local current_pass = GetVariable("current_pass") or "onedaypass"
	
	Execute("mapper goto 38714")
	
    if current_pass == "250usespass" then
        SendNoEcho("buy 250usespass")
    elseif current_pass == "oneweekpass" then
        SendNoEcho("buy oneweekpass")
    elseif current_pass == "onedaypass" then
        SendNoEcho("buy onedaypass")
    end

    SendNoEcho("keyring put pass")
    Note("[SlopeTrain] Pass purchased. Returning to Pinnacle...")
    Execute("mapper goto " .. ROOM_PINNACLE)
end

function set_slope_message()
    local messages = utils.editbox(
        "Custom slope messages.\nUse %s as a substitute for direction.\nOne custom message per line.\nTo prevent color bleeding end with $C.",
        "Slope Notifications",
        table.concat(train_msg, "\r\n")
    )

    if not messages then
        Note("[SlopeTrain] No custom messages or user cancelled. Keeping existing messages.")
        return
    end

    local new_messages = {}
    for line in messages:gmatch("[^\r\n]+") do
        table.insert(new_messages, line)
    end

    if #new_messages == 0 then
        Note("[SlopeTrain] No messages entered. Reverting to default messages.")
        new_messages = {
            "@C(@DSlopeTrain@C) @x256CHOO @x256CHOO@W, the train is moving @C%s@W!$C",
            "@C(@DSlopeTrain@C) @WALL ABOARD! The train moving @C%s@W... @x256CHOO @x256CHOO@W!$C"
        }
    end

    train_msg = new_messages
    SetVariable("train_msg", json.encode(train_msg))  -- ✅ JSON-safe, keeps symbols intact
    Note("[SlopeTrain] Saved custom messages.")
end

function set_channel(name, line, wildcards)
    SetVariable("chan", wildcards[1])
    cnote("@b[SlopeTrain] Changed report channel to:@C " .. wildcards[1])
end

last_train_msg = nil

function repeat_train_direction()
  if not is_in_icefall then
    Note("[SlopeTrain] You can only repeat directions in Icefall.")
    return
  end
  
  if last_train_msg then
     SendNoEcho("gtell " .. last_train_msg)
    Note("[SlopeTrain] Repeating last direction")
  else
    Note("[SlopeTrain] No previous direction to repeat.")
  end
end

function toggle_pass_type()
    local current_pass = GetVariable("current_pass") or "onedaypass"
    local pass_ids = {"250usespass", "oneweekpass", "onedaypass"}
    local current_index = 0

    for i, pass in ipairs(pass_ids) do
        if pass == current_pass then
            current_index = i
            break
        end
    end

    local next_index = (current_index % #pass_ids) + 1
    local next_pass = pass_ids[next_index]
    SetVariable("current_pass", next_pass)

    local display_formats = {
        ["250usespass"] = "@W[@G250 Uses @RPass@W]",
        oneweekpass    = "@W[@COne Week @RPass@W]",
        onedaypass     = "@W[@yOne Day @RPass@W]"
    }

    cnote(string.format("@b[SlopeTrain] Pass type toggled. Current pass type: %s", display_formats[next_pass]))
end

function show_current_pass()
    local current_pass = GetVariable("current_pass") or "onedaypass"

    local display_formats = {
        ["250usespass"] = "@W[@G250 Uses @RPass@W]",
        oneweekpass    = "@W[@COne Week @RPass@W]",
        onedaypass     = "@W[@yOne Day @RPass@W]"
    }

    cnote(string.format("@b[SlopeTrain] Current pass type: %s", display_formats[current_pass] or current_pass))
end

function toggle_slope_repop()
    if slope_repop_enabled then
        slope_repop_enabled = false
		EnableGroup("slopeRepop", false)
        cnote("@b[SlopeTrain]Repop message is now @ROFF")
    else
        slope_repop_enabled = true
		EnableGroup("slopeRepop", true)
        cnote("@b[SlopeTrain]Repop message is now @GON")
    end
end

function slope_repop()
  if not chan or chan == "" then
    ColourNote("red", "", "SlopeTrain: Channel is not set. Use 'slopetrain channel' to set one.")
    return
  end
  
  if is_group_leader then
  SendNoEcho(chan .. " @C(@x244SlopeTrain@C) @WThe engine just got @GFUELED@W. @x256C@x256H@x256U@x256G@x256G@x256A @x256C@x256H@x256U@x256G@x256G@x256A @x256C@x256H@x256O@x256O @x256C@x256H@x256O@x256O$C")
  end
end

function show_helpfile()
  cnote("")
  cnote("@W                 @RIMPORTANT: SETUP YOUR CEXITS!                 @w")  
  cnote("@C+@W-------------------[@CSlopeTrain  Helpfile@W]--------------------@C+@w")
  ColourNote("white", "", "", "white", "", "")
  ColourNote("lightcyan", "", "slopetrain update check   - ", "white", "", "Check your current version")
  ColourNote("lightcyan", "", "slopetrain update install - ", "white", "", "Install latest version")
  ColourNote("lightcyan", "", "slopetrain help           - ", "white", "", "Outputs this helpfile")
  ColourNote("white", "", "", "white", "", "")
  cnote("@C+@W-------------------[@CSlopeTrain Setup@W]------------------------@C+@w")
  ColourNote("white", "", "", "white", "", "")
  ColourNote("lightcyan", "", "slopetrain set msg        - ", "white", "", "Setup your own slope mesesages")
  ColourNote("lightcyan", "", "slopetrain set pass       - ", "white", "", "Toggle between type of pass")
  ColourNote("lightcyan", "", "slopetrain show pass      - ", "white", "", "Shows your current set pass")
  ColourNote("lightcyan", "", "slopetrain repop          - ", "white", "", "Turn on/off the repop message") 
  ColourNote("lightcyan", "", "slopetrain channel        - ", "white", "", "Set repop channel: ", "cyan", "", chan)
  ColourNote("white", "", "", "white", "", "")
  cnote("@C+@W-------------------[@CSlopeTrain Commands@W]---------------------@C+@w")
  ColourNote("white", "", "", "white", "", "")
  ColourNote("lightcyan", "", "icee                      - ", "white", "", "Move the train to the East slope")
  ColourNote("lightcyan", "", "ices                      - ", "white", "", "Move the train to the South slope")
  ColourNote("lightcyan", "", "icew                      - ", "white", "", "Move the train to the West slope")
  ColourNote("lightcyan", "", "icenxt                 - ", "white", "", "To move to the next room on slope")  
  ColourNote("lightcyan", "", "repdir                    - ", "white", "", "Manually report the last direction")
  ColourNote("white", "", "", "white", "", "")
  cnote("@C+@W-------------------[@CSlopeTrain Helpfile@W]---------------------@C+@w")
  cnote("")
end

    ]]>
  </script>

  <triggers>
    <!-- Trigger for leader entering slingshot -->
    <trigger
      match="^([^ ]+) enters a gigantic slingshot, ready for another trip down the Avalanche Slalom!$"
      name="slingshot"
      enabled="y"
      group="slopeTrain"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="follow_the_leader"
    />


    <!-- Trigger for Iced Fish Hatchery -->
    <trigger
      match="^([^ ]+) enters Iced Fish Hatchery.$"
      name="hatchery"
      enabled="y"
      group="slopeTrain"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="follow_the_leader"
    />

    <!-- Trigger for Chutes and Empty Bladders -->
    <trigger
      match="^([^ ]+) enters Chutes and Empty Bladders.$"
      name="chutes"
      enabled="y"
      group="slopeTrain"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="follow_the_leader"
    />

    <!-- Trigger for King of the Kill -->
    <trigger
      match="^([^ ]+) enters King of the Kill.$"
      name="kill"
      enabled="y"
      group="slopeTrain"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="follow_the_leader"
    />
	
    <!-- Trigger for expired pass -->
    <trigger
      name="ExpiredPass"
      match="^A gruff ski patrol destroys your expired pass\!|You must have a ski pass in order to enter the powerup slopes\.$"
      enabled="y"
      group="slopeTrain"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="expired_pass_action"
    />
	
    <!-- Trigger for repop -->
    <trigger
      name="Repop"
      match="^The Avalanche Slalom is covered with dry powder snow\.$"
      enabled="y"
      group="slopeRepop"
      regexp="y"
      send_to="12"
      ignore_case="y"
      sequence="100"
      script="slope_repop"
    />	
  </triggers>

</muclient>


